(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const vid=url=>{const m=String(url||'').match(/(?:video\/|v=|youtu\.be\/)([A-Za-z0-9_-]{6,})/);return m&&m[1]};const tt=url=>{const m=String(url||'').match(/\/video\/(\d+)/);return m&&m[1]};const fmt=n=>{if(!Number.isFinite(n))return'0:00';const m=Math.floor(n/60),s=Math.floor(n%60);return m+':'+String(s).padStart(2,'0')};const playIcon='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7z"/></svg>',pauseIcon='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M7 5h4v14H7zm6 0h4v14h-4z"/></svg>',volIcon='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 9v6h4l5 4V5L8 9H4zm12.5 3a4.5 4.5 0 0 0-2.2-3.9v7.8a4.5 4.5 0 0 0 2.2-3.9z"/></svg>';function platformLinks(r){const p=r.platforms||{},names={spotify:'spotify',apple:'apple music',youtube:'youtube',soundcloud:'soundcloud'};let links=Object.keys(names).filter(k=>p[k]).map(k=>`<a class="platformLink" target="_blank" rel="noopener" href="${esc(p[k])}">${names[k]}</a>`).join('');if(!links&&(r.source_url||r.presave_url))links=`<a class="platformLink" target="_blank" rel="noopener" href="${esc(r.source_url||r.presave_url)}">все площадки</a>`;return links}function playerHtml(title,url,id,label='preview'){if(!url)return`<div class="customPlayer noAudio"><div class="playerMeta"><span>audio</span><b>preview пока не найден</b></div></div>`;return`<div class="customPlayer" data-player="${esc(id)}"><audio preload="metadata" src="${esc(url)}"></audio><button class="playBtn" type="button" aria-label="воспроизвести">${playIcon}</button><div class="playerMain"><div class="playerTop"><b>${esc(title)}</b><span>${esc(label)}</span></div><div class="progress" role="slider" tabindex="0"><i></i></div><div class="playerBottom"><div class="time"><span class="cur">0:00</span><span class="dur">0:00</span></div><label class="volume" aria-label="громкость">${volIcon}<input type="range" min="0" max="1" step="0.01" value="0.82"></label></div></div></div>`}function releaseCard(r,i){const isMulti=/album|ep/i.test(r.type||'')||((r.tracklist||[]).length>1),tracks=Array.isArray(r.tracklist)?r.tracklist:[];let body='';if(tracks.length){body=`<div class="trackList">${tracks.map((t,j)=>`<div class="trackRow"><span class="trackNo">${String(j+1).padStart(2,'0')}</span>${playerHtml(t.title||t.trackName||`трек ${j+1}`,t.preview||t.previewUrl||'',`${i}-${j}`,'preview')}</div>`).join('')}</div>`}else if(isMulti){body='<div class="albumLoading">подгружаю треклист…</div>'}else body=playerHtml(r.title,(r.platforms||{}).preview||'',`${i}-0`);return`<article class="releaseCard" data-release-index="${i}">${r.cover_url?`<img class="releaseCover" src="${esc(r.cover_url)}" alt="">`:`<div class="releaseCover"></div>`}<div class="releaseRight"><div class="releaseInfo"><div><div class="releaseDateSmall">${new Date(r.release_date).toLocaleDateString('ru-RU')} · ${esc(r.type||'release')}</div><h3>${esc(r.title)}</h3></div><div class="platformRow">${platformLinks(r)}</div></div><div class="playerArea">${body}</div></div></article>`}function bindPlayers(root){root.querySelectorAll('.customPlayer[data-player]:not([data-bound])').forEach(w=>{w.dataset.bound='1';const a=w.querySelector('audio'),b=w.querySelector('.playBtn'),bar=w.querySelector('.progress'),fill=bar.querySelector('i'),cur=w.querySelector('.cur'),dur=w.querySelector('.dur'),vol=w.querySelector('.volume input');a.volume=Number(vol?.value||.82);const setState=playing=>{b.innerHTML=playing?pauseIcon:playIcon;b.setAttribute('aria-label',playing?'пауза':'воспроизвести');w.classList.toggle('playing',playing)};const draw=()=>{const d=a.duration||0,p=d?Math.min(1,a.currentTime/d):0;fill.style.width=(p*100)+'%';cur.textContent=fmt(a.currentTime);dur.textContent=fmt(d)};a.addEventListener('loadedmetadata',draw);a.addEventListener('timeupdate',draw);a.addEventListener('ended',()=>{setState(false);draw()});b.onclick=async()=>{document.querySelectorAll('.customPlayer audio').forEach(x=>{if(x!==a&&!x.paused){x.pause();const q=x.closest('.customPlayer');if(q){q.classList.remove('playing');const qb=q.querySelector('.playBtn');if(qb)qb.innerHTML=playIcon}}});if(a.paused){try{await a.play();setState(true)}catch{}}else{a.pause();setState(false)}};const seek=e=>{const r=bar.getBoundingClientRect(),x=Math.max(0,Math.min(1,(e.clientX-r.left)/r.width));if(Number.isFinite(a.duration))a.currentTime=x*a.duration};bar.addEventListener('pointerdown',seek);bar.addEventListener('keydown',e=>{if(e.key==='ArrowRight')a.currentTime=Math.min(a.duration||0,a.currentTime+5);if(e.key==='ArrowLeft')a.currentTime=Math.max(0,a.currentTime-5)});if(vol)vol.oninput=()=>a.volume=Number(vol.value);draw()})}async function appleAlbumTracks(r){const term=`inkivi ${r.title}`;const sr=await fetch('https://itunes.apple.com/search?'+new URLSearchParams({term,entity:'album',limit:'20',country:'ru'}));if(!sr.ok)return[];const sj=await sr.json(),rows=sj.results||[],exact=rows.find(x=>String(x.artistName||'').toLowerCase()==='inkivi'&&String(x.collectionName||'').toLowerCase()===String(r.title||'').toLowerCase())||rows.find(x=>String(x.artistName||'').toLowerCase()==='inkivi');if(!exact?.collectionId)return[];const lr=await fetch('https://itunes.apple.com/lookup?'+new URLSearchParams({id:String(exact.collectionId),entity:'song',country:'ru'}));if(!lr.ok)return[];const lj=await lr.json();return(lj.results||[]).filter(x=>x.wrapperType==='track').map(x=>({title:x.trackName,preview:x.previewUrl||'',duration:x.trackTimeMillis||0,number:x.trackNumber||0})).sort((a,b)=>a.number-b.number)}async function hydrateAlbums(root,rows){await Promise.all((rows||[]).map(async(r,i)=>{if(!/album|ep/i.test(r.type||'')||((r.tracklist||[]).length>1))return;const card=root.querySelector(`[data-release-index="${i}"]`),slot=card?.querySelector('.playerArea');if(!slot)return;try{const tracks=await appleAlbumTracks(r);if(tracks.length){r.tracklist=tracks;slot.innerHTML=`<div class="trackList">${tracks.map((t,j)=>`<div class="trackRow"><span class="trackNo">${String(j+1).padStart(2,'0')}</span>${playerHtml(t.title,t.preview,`${i}-${j}`,'preview')}</div>`).join('')}</div>`;bindPlayers(slot)}else slot.innerHTML=playerHtml(r.title,(r.platforms||{}).preview||'',`${i}-0`)}catch{slot.innerHTML=playerHtml(r.title,(r.platforms||{}).preview||'',`${i}-0`)}}))}function visualCard(v,i){const id=v.platform==='tiktok'?tt(v.url):null;if(id)return`<article class="visualCard visualTikTok" data-visual="${i}"><div class="visualMedia"><iframe class="tiktokFrame" loading="lazy" allow="encrypted-media;fullscreen" src="https://www.tiktok.com/player/v1/${id}?music_info=1&description=1&autoplay=0"></iframe></div><div class="visualCaption">${esc(v.title||'TikTok')}<div class="visualMeta">TikTok</div></div></article>`;const y=vid(v.url);if(v.platform==='youtube'&&y)return`<article class="visualCard visualYouTube" data-visual="${i}"><div class="visualMedia"><iframe class="ytFrame" loading="lazy" allow="accelerometer;autoplay;clipboard-write;encrypted-media;gyroscope;picture-in-picture;web-share" allowfullscreen src="https://www.youtube.com/embed/${esc(y)}"></iframe></div><div class="visualCaption">${esc(v.title||'YouTube')}<div class="visualMeta">YouTube</div></div></article>`;return`<article class="visualCard visualOther" data-visual="${i}"><a class="visualFallback" target="_blank" rel="noopener" href="${esc(v.url)}">${v.preview_url?`<img src="${esc(v.preview_url)}" alt="">`:''}<div class="visualCaption">${esc(v.title||v.platform||'визуал')} →<div class="visualMeta">${esc(v.platform||'visual')}</div></div></a></article>`}function install(){const hero=document.querySelector('.hero');if(hero&&!document.getElementById('heroReleaseCover')){const img=document.createElement('img');img.id='heroReleaseCover';img.className='heroReleaseCover';img.alt='';img.hidden=true;const title=document.getElementById('releaseTitle');title?.after(img)}document.querySelectorAll('[data-panel]').forEach(b=>{b.onclick=()=>{const key=b.dataset.panel;pt.textContent=key;if(key==='релизы'){px.innerHTML=`<div class="releaseListRich">${(releaseRows||[]).map(releaseCard).join('')||'релизов пока нет'}</div>`;bindPlayers(px);hydrateAlbums(px,releaseRows||[])}else if(key==='визуалы')px.innerHTML=`<div class="visualGridRich">${(visualRows||[]).map(visualCard).join('')||'визуалов пока нет'}</div>`;else px.textContent='Здесь будет браузерная игра.';panel.classList.add('on');refreshGlyphs(pt)}});const oldLoad=loadContent;loadContent=async function(){await oldLoad();const now=Date.now(),rows=releaseRows||[],future=rows.filter(r=>new Date(r.release_date).getTime()>now).sort((a,b)=>new Date(a.release_date)-new Date(b.release_date)),current=future[0]||rows[0],img=document.getElementById('heroReleaseCover');if(img){if(current?.cover_url){img.src=current.cover_url;img.hidden=false}else img.hidden=true}};loadContent()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install()})();